import React from "react"
import "./buttons.scss";

const PublishBtn = (props) => {
    return (
        <button className="buttons">
            Publish
        </button>
    )
};

export default PublishBtn;
